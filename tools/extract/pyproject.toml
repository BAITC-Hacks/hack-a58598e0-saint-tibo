[project]
name = "saint-tibo-extract"
version = "0.1.0"
description = "Local protocol extraction stage: action items and summary from a transcript"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["extract"]
