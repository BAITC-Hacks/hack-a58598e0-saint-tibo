[project]
name = "saint-tibo-diarize"
version = "0.1.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "numpy==2.5.3",
    "sherpa-onnx==1.13.8",
    # Upstream wheel metadata requires core; pin explicitly for uv's sdist resolution.
    "sherpa-onnx-core==1.13.8",
]

[tool.uv]
package = false
