# Explicit DEVELOPMENT ONLY. Synthetic fixtures only; this sends prompts to OpenAI.
# Activate with: docker compose --profile openai-dev --env-file .env -f compose.yaml -f dev/openai-cloud.compose.yaml up
OPENAI_DEV_API_KEY=replace-me
OPENAI_DEV_BASE_URL=https://api.openai.com/v1
OPENAI_DEV_CHAT_MODEL=replace-me
OPENAI_DEV_EMBEDDING_MODEL=replace-me
